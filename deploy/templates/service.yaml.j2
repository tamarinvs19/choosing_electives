apiVersion: v1
kind: Service
metadata:
  name: {{ app_name }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ remote_port | default(80) }}
      targetPort: {{ app_port }}
  selector:
    app: {{ app_name }}